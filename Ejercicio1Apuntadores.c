#include <stdio.h>
#include <stdlib.h>

int main(){
    char *pString;
    int n=0;
    pString = (char*)malloc(sizeof(char)*50);
    printf("Ingrese la cadena: \n");
    for(int i=0; i<50; i++){
        
        if(*(p+i) == "\0"){
            break;
        }
    }

    
    printf("\nNumero de caracteres es: %i", i);

    return 0;
}